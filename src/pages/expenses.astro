---
import Layout from '../layouts/Layout.astro'
import HeadComponent from '../components/table/Head.astro'
import ExpenseRow from '../components/table/Expense.astro'
---

<Layout title="Expense Tracker">
	<main class="mt-9 w-full bg-white [grid-area:main] dark:bg-slate-800">
		<table class="mt-1 w-full table-fixed rounded-lg text-left text-sm shadow-lg">
			<HeadComponent isLoan={false}/>
			<tbody>
				<ExpenseRow
					date={new Date()}
					description="Test expense AND A LONGONGONGONONONOGNONGONGOGNOGN DESCRIPTION"
					amount={699.99}
					category={'ENTERTAINMENT'.toUpperCase()}
				/>
				<ExpenseRow
					date={new Date()}
					description="Test expense AND A LONGONGONGONONONOGNONGONGOGNOGN DESCRIPTION"
					amount={699.99}
					category={'FOOD'.toUpperCase()}
				/>
				<ExpenseRow
					date={new Date()}
					description="Test expense AND A LONGONGONGONONONOGNONGONGOGNOGN DESCRIPTION"
					amount={699.99}
					category={'HEALTH'.toUpperCase()}
				/>
				<ExpenseRow
					date={new Date()}
					description="Test expense AND A LONGONGONGONONONOGNONGONGOGNOGN DESCRIPTION"
					amount={699.99}
					category={'TRANSPORTATION'.toUpperCase()}
				/>
				<ExpenseRow
					date={new Date()}
					description="Test expense AND A LONGONGONGONONONOGNONGONGOGNOGN DESCRIPTION"
					amount={699.99}
					category={'TRANSPORTATION'.toUpperCase()}
				/>
				<ExpenseRow
					date={new Date()}
					description="Test expense AND A LONGONGONGONONONOGNONGONGOGNOGN DESCRIPTION"
					amount={699.99}
					category={'TRANSPORTATION'.toUpperCase()}
				/>
			</tbody>
		</table>
	</main>
</Layout>

<style></style>
